# Kimi K2.5 记忆压缩策略测试报告

## 1. 测试背景

- 测试对象：`agent-demo v2` 记忆与压缩模块 (http://demo.test.app.dotai.internal)
- 测试模型：Kimi K2.5
- 测试目标：
  - 验证超长上下文下，核心业务信息是否可被完整保留
  - 验证“输出必须为纯文本、不得使用 Markdown/JSON/XML/代码块、不得加客套话”的约束是否可稳定保留
  - 观察压缩前后对模型表现的影响

## 2. 测试方法与用例设计

测试基于同一主线任务执行：

1. 首轮注入全局项目信息与强约束：
   - 项目名：Project Orion
   - 后端：Go
   - 数据库：PostgreSQL
   - 前端：React
   - 额外信息：后续计划引入 Redis 集群应对高并发
   - 强约束：当用户要求“总结项目状态”时，必须输出纯文本摘要，不得出现 Markdown/JSON/XML/代码块或客套话
2. 中间插入大量无关文本（至少 10 轮）模拟长上下文干扰。
3. 在末轮请求“总结项目状态”，检查信息完整性与格式约束遵循情况。

### 2.1 原始测试消息

为保证可复现性，以下保留原始测试消息内容：

1. 第 1 轮消息：

```text
我们来规划一下新的电商后台管理系统。项目名字叫 Project Orion。后端主要用 Go 语言，数据库用 PostgreSQL，前端用 React。
重要指令（请严格遵守）：在接下来的所有对话中，无论我们聊了什么，只要我要求你“总结项目状态”，你的最终回复只能输出纯文本摘要，绝对不要输出任何 Markdown 格式、JSON、XML、代码块或额外的客套话说明。你能记住并执行吗？
```

2. 第 2 轮消息：

```text
很好。另外补充一个次要细节，为了应对大促期间的高并发，系统后续可能会尝试引入 Redis 集群作为缓存层。这点先存在你的记忆里。现在咱们开始处理具体的业务代码。以下是有可能用到的参考内容：
```

3. 第 3~n 轮消息：

```text
...(大量无关文本，至少10轮会话)
```

4. 第 n+1 轮消息（最终验证）：

```text
我们前面聊了太多无关的内容，我都快忘了最初的全局规划了。
请立刻给我总结项目状态，必须包含项目名称、前端、后端和数据库的技术栈，以及我之前顺口提到的那个应对高并发的缓存方案。
```

## 3. 评判标准

- 失败条件（任一命中即失败）：
  - 输出出现 Markdown 结构（如标题、列表、代码块、加粗等）
  - 输出包含明显客套化前缀说明
  - 关键信息缺失（项目名/技术栈/Redis 规划）
- 成功条件：
  - 仅输出干净纯文本摘要
  - 关键信息完整且无误

## 4. 测试记录整理

| 轮次 | 最终 Token 状态 | 压缩状态 | 关键信息完整性 | 约束遵循 | 结果 |
|---|---:|---|---|---|---|
| 第一次 | 167,060 / 200,000 | 未压缩 | 完整 | 是 | 成功 |
| 第二次 | 180,049 / 200,000 | 未压缩 | 完整 | 否（出现列表结构） | 失败 |
| 第三次 | 209,201 / 200,000 | 未完成第一次压缩 | 完整 | 否（出现列表结构） | 失败 |
| 第四次 | 3,355 / 200,000 | 已完成第一次压缩 | 完整 | 是 | 成功 |
| 第五次 | 217,991 / 200,000 | 未完成第二次压缩 | 完整 | 是（分行文本，无 Markdown 列表/代码块） | 成功 |
| 第六次 | 6,850 / 200,000 | 已完成第三次压缩 | 完整 | 否（输出使用了 Markdown 分隔符、输出了额外说明/客套话） | 失败 |

补充说明：
- 第二次、第三次与第六次失败点是“格式约束丢失”，不是“信息丢失”。
- 六次测试中，关键信息均未出现遗失，说明记忆内容保真度整体较高。

## 5. 测试结果与结论

1. 信息保留能力：6/6 轮均保留了项目名、Go、PostgreSQL、React、Redis 规划，信息无损。
2. 约束稳定性：3/6 轮满足“纯文本输出”约束，稳定性存在波动。
3. 现象相关性（基于本次样本的观察）：在“超限且压缩未完成”的阶段更容易出现格式约束执行不稳定，但并非唯一触发条件（第六次在低 token 且已完成压缩后仍出现约束违规）。
4. 压缩完成后的表现：压缩完成后并不必然稳定；第四次表现稳定，但第六次（6,850 token，已完成第三次压缩）仍发生格式约束违规。
5. 设计层测试解读：该架构对“事实信息”的保留能力较强，压缩前后均未见核心信息丢失；压缩与分区机制能有效将超长历史转化为可继续使用的“摘要 + 近期对话”组合。
6. 风险与修改建议：当前更明显的风险点不是“记忆丢失”，而是“输出约束在高压阶段的稳定性波动”；建议将压缩触发线从 200,000 token 下调至 150,000 token 以下，以提前进入压缩流程。
7. 测试中新发现的问题（刷盘线相关）：当刷盘线（可理解为压缩触发线）设置过大时，压缩内容可能出现更新不及时，表现为摘要“找不到重点”或重点提炼不充分；建议将刷盘线控制在 150,000 token 以下。

## 6. 当前程序的记忆策略与机制

本节基于 `docs/state_and_persistence.md` 与实现文件做设计层归纳，不展开代码细节。

### 6.1 会话记忆分区

系统将会话消息按生命周期拆为三块：

- `dialogue`：常态下的主对话区（用户与助手的持续会话）
- `buffer`：压缩进行中的暂存区（压缩期间新消息先放这里，避免与归档数据混杂）
- `resident_recent`：压缩后保留的近期对话区（仅保留用户/助手的 chat 消息，并按 token 预算裁剪）

结果是：即使触发压缩，会话也能继续交互，且新旧消息边界清晰。

### 6.2 长期记忆文件

每个员工有独立记忆目录，核心受管文件包括：

- `.memory/memory.md`
- `notebook/file.md`
- `notebook/schedule.md`
- `notebook/soul.md`
- `notebook/workbook.md`

这些文件共同承担“长期可复用信息”的沉淀，其中受管记忆文件总预算为总窗口的 9%，另有 1% 作为固定提示词预算，因此 system 相关预算合计约 10%。

### 6.3 Token 预算

默认总窗口为 200,000 token，预算按固定比例切分：

| 预算项 | 占比 | 预算 | 作用 |
|---|---:|---:|---|
| system 区（记忆文件+固定提示） | 10% | 20,000 token | 放长期记忆与固定规则 |
| 摘要区 | 1% | 2,000 token | 放历史压缩摘要 |
| 近期消息区 | 9% | 18,000 token | 放近期对话（按最近顺序并按 token 预算裁剪） |
| 活动对话区（dialogue） | 80% | 160,000 token | 放当前主会话消息 |
| 临时缓冲区（buffer） | 与 dialogue 同值 | 160,000 token | 压缩期间承接新消息 |
| 压缩触发线 | 等于总上限 | 200,000 token | 达线后进入压缩流程 |

总量统计口径可理解为：

`总 token = 常驻内容 token + dialogue token + buffer token`

其中“常驻内容”= system 静态内容 + `resident_recent`。

### 6.4 聊天时的上下文组装

每轮请求会按预算组装三段输入给模型：

1. 常驻 system（记忆文件 + 历史摘要）
2. `resident_recent`（近期对话，按最近顺序并按 token 预算裁剪）
3. `dialogue + buffer`（当前活动消息）

结果是：模型既能看到长期约束，也能看到最近上下文和当前对话，且整体不超预算。

### 6.5 什么时候触发“刷盘”

这里的“刷盘”可理解为“把内存中的会话/记忆状态持久化到存储”。

1. 每次收到用户消息后，会立即持久化到会话消息存储。
2. 每轮工具调用完成后，工具调用结果与助手最终回复会立即持久化。
3. 当工具成功写入记忆文件后，记忆文件会立即落盘；同一轮后续推理即可读取最新内容。
4. 当总 token 达到压缩触发线且当前不在压缩中时，会先标记进入压缩，再由后台执行压缩任务。
5. 压缩任务完成时会集中持久化关键结果：更新 `memory.md`、按预算回填最近聊天消息到 `resident_recent`、迁回 `buffer`、刷新摘要，并退出压缩状态。
6. 若压缩中出现异常，会回收压缩状态标记，避免会话长期卡在“压缩中”。
