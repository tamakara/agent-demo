# Agent 记忆压缩策略：滚动摘要 (Rolling Summary)

## 一、 上下文分区

整个大模型上下文窗口严格且仅划分为两部分：

1. **系统提示词**：静态与全局约束区。包含系统指令、笔记数据（notebook），以及**压缩记忆（memory）**。
2. **对话区**：动态交互区。存放用户与助手的原始聊天记录。

## 二、 核心概念

1. **压缩窗口**：对话区内排在最前面的（旧的）消息，通常对应 `0` 到 **压缩线** token 范围的历史记录。它是每次触发压缩时，提取出来供大模型进行总结归约的目标数据源。
2. **压缩线**：触发后台异步压缩的 token 阈值。当对话区的长度超过此线时，系统即刻启动压缩流程。
3. **记忆容量**：系统提示词中 `memory` 的固定大小上限。在滚动摘要过程中，生成的新记忆容量绝对不能超过该数值，这就要求大模型在压缩时必须进行强制的优先级取舍。
4. **留存比例**：压缩完成后，对“压缩窗口”内的历史消息进行安全裁剪时，保留尾部消息的比例。其目的是裁剪掉前部的冗余长文本，同时留存尾部记录，作为新旧上下文之间的平滑过渡。

## 三、 运行流程与机制

系统会在**每轮对话完成后**或**每次压缩完成后**进行总长度检测，并按以下生命周期流转：

1. **异步触发**：当 `对话区长度 > 压缩线` 时，对“压缩窗口”内的数据触发后台异步压缩，主线程继续保持对用户的无感交互。
2. **锁定压缩窗口**：确定对话区前 `0` 到 **压缩线** 范围内的消息，锁定这批消息的边界（如 message_id 游标），防止异步执行期间用户发送的新消息在后续步骤中被误删。
3. **强制取舍压缩 (LLM 调用)**：后台 Worker 将“旧的 memory”与“压缩窗口内的消息”组合为 Prompt。通过明确的指令，强迫 LLM 在不超过**记忆容量**的前提下，对信息进行优先级排序和无情舍弃，输出全新的总结。
4. **更新与裁剪**：
   - 压缩成功后，使用 LLM 输出的新摘要直接覆盖系统提示词中的旧 `memory`。
   - 依据锁定的游标和**留存比例**，对“压缩窗口”内已经被总结过的历史消息进行裁剪清理。
5. **动态回测**：清理操作完成后，系统自动进行下一次长度检测。若清理后对话区仍超压缩线，则无缝启动下一轮压缩流程；若整体水位已回落至安全区间，则结束本次流转。